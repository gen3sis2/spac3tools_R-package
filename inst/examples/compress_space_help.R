\dontrun{
library(spac3tools)

# The function compress a gen3sis2 spaces.rds
compress_space(
  dir_input = "your/spaces/directory", # the dir containing the spaces.rds
  dir_output = "your/compressed_spaces/directory" # the dir to save the compressed spaces.rds
)
}
