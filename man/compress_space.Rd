% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_compress.R
\name{compress_space}
\alias{compress_space}
\title{Compress a gen3sis2 Space object by removing cost distances}
\arguments{
\item{dir_input}{Directory containing the `spaces.rds` to compress.}

\item{dir_output}{Directory where the compressed version will be saved.
If `NULL`, a temporary directory is used.}
}
\value{
Saves a compressed `spaces.rds` file to `dir_output`. Does not return an R object.
}
\description{
This function removes large cost distance matrices from a `spaces.rds` file,
creating a lighter version suitable for storage and distribution. All other
metadata and environmental data are preserved.
}
\examples{
\dontrun{
library(spac3tools)

# The function compress a gen3sis2 spaces.rds
compress_space(
  dir_input = "your/spaces/directory", # the dir containing the spaces.rds
  dir_output = "your/compressed_spaces/directory" # the dir to save the compressed spaces.rds
)
}
}
