% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_space.R
\name{data_raster_to_h3}
\alias{data_raster_to_h3}
\title{Creates a list of H3 data.frame}
\usage{
data_raster_to_h3(h3_address = NULL, res = NULL, raster_list)
}
\arguments{
\item{h3_address}{character. A set of H3 cell addresses. If provided
together with \code{res}, the resolution is ignored.}

\item{res}{integer. H3 resolution at which to generate cells covering the
globe. Only used if \code{h3_address} is \code{NULL}.}

\item{raster_list}{list of named list(s) of raster(s) or raster file(s) name(s). Starting from the past towards the present.
NOTE: the list names are important since these are the environmental names}
}
\value{
A named list of data frames. Each data frame corresponds to one
  element of \code{raster_list} and contains:
  \itemize{
    \item the H3 cell identifier,
    \item extracted raster values for each layer,
    \item centroid coordinates (\code{x}, \code{y}).
  }
}
\description{
Creates a list of H3 data.frame
}
\examples{
# TODO
}
